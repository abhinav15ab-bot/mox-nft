[project]
name = "moccasin_project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
[tool.coverage.run]
source = ["src"]

[tool.coverage.report]
omit = [
    "lib/*",
    "script/*",
    "tests/*"
]
